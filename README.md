# 🏫 서경대학교 2025-2 Cloud Computing  
## 📂 Report / Project Repository

---

### 📑 소개
이 레포지토리는 **2025년 2학기 클라우드컴퓨팅 수업**에서 작성한  
과제(Report) 및 프로젝트 코드를 정리한 저장소입니다.

---

## 🖥️ Monitoring - 리소스 기반 로드 밸런싱 시스템

클라이언트의 시스템 리소스를 모니터링하고, 가장 여유있는 클라이언트에게 작업을 할당하는 로드 밸런싱 시스템입니다.

### ✨ 주요 기능

- **리소스 모니터링**: CPU, 메모리, 네트워크, 스토리지 사용량 실시간 수집
- **자동 로드 밸런싱**: 남은 리소스량을 계산하여 가장 여유있는 클라이언트 선택
- **작업 할당**: 선택된 클라이언트에게 작업 자동 분배
- **실시간 상태 확인**: 연결된 모든 클라이언트의 리소스 현황 조회

### 📋 구성 요소

#### 1. `ReceiveResourceMessage.py` (서버)
- 클라이언트로부터 리소스 정보 수신 및 저장
- 리소스 점수 계산 (CPU 70% + 메모리 30% 가중치)
- 가장 여유있는 클라이언트에게 작업 할당

**명령어:**
- `task` - YouTube 열기 작업을 가장 여유있는 클라이언트에게 할당
- `status` - 연결된 모든 클라이언트의 리소스 현황 출력
- `quit` - 서버 종료

#### 2. `SendResoureMessage.py` (클라이언트)
- 주기적으로 자신의 리소스 정보를 서버에 전송
- 서버로부터 작업 명령 수신 대기 (포트 9998)
- 작업 명령 수신 시 자동 실행

### 🚀 사용 방법

#### 1. 서버 실행
```bash
python Monitoring/ReceiveResourceMessage.py [포트번호]
```
기본 포트: 9999

#### 2. 클라이언트 실행 (여러 머신에서)
`SendResoureMessage.py`의 `server_ip`를 서버 IP로 수정 후:
```bash
python Monitoring/SendResoureMessage.py
```

#### 3. 작업 할당
서버 콘솔에서 `task` 명령어 입력 시, 가장 여유있는 클라이언트가 자동으로 선택되어 YouTube 웹페이지가 열립니다.

### 📊 로드 밸런싱 알고리즘

```
리소스 점수 = (100 - CPU 사용률) × 0.7 + (10000 - 메모리 사용량) / 100 × 0.3
```

점수가 높을수록 남은 리소스가 많아 작업 할당에 우선 선택됩니다.

### 🔧 필요 패키지
- Python 3.x
- 표준 라이브러리: `socket`, `threading`, `subprocess`, `webbrowser`

---